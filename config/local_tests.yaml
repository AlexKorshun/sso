env: "local"
database_url: "postgres://postgres_user:secret@localhost:5433/sso?sslmode=disable"
token_ttl: 1h
grpc:
  port: 44044
  # используется в tests/suite как таймаут контекста на весь тест
  timeout: 5s
